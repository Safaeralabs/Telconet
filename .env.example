# URL pública del sitio en producción (usada para metadatos SEO / Open Graph).
# En Vercel, defínela en Project Settings → Environment Variables cuando tengas el dominio.
# Si no se define, en Vercel se usa automáticamente VERCEL_URL, y en local http://localhost:3000
NEXT_PUBLIC_SITE_URL=https://tu-dominio.com
